/* UI candidate compatibility stub.
   The frozen toolkit intentionally excludes Standard 10 external assets.
   The parent HTML's inline ra65-cases payload remains the source used by the workbench. */
