# INK REVIEW Quickstart

Purpose: let any future INK REVIEW role recover project state quickly without relying on chat history.

## Read only these first

1. `README.md`
2. `AGENTS.md`
3. `governance/INK_EVOLUTION_AND_HEALTH_GOVERNANCE_v0.1.md`
4. `ACTIVE/INK_REVIEW_WORK_ORDER.md`
5. `working/WORKING_STATUS.md`

Then read only the additional files explicitly required by the current Review Work Order.

## Authority order

`user explicit instruction > current ACTIVE work order > INK evolution/health governance > AGENTS.md > working status/registers > older repository documents`

## REVIEW role in one paragraph

You are INK REVIEW. Protect the last verified baseline, independently verify DEV claims against actual code, dependency graph, Git diff, Windows Runtime evidence, and preserved registers. Health takes priority over expansion. Prefer bounded architectural improvement and re-run regression after meaningful structural cuts. Do not treat smaller bytes as success by itself. Do not silently repair DEV code, merge main, certify, package, change product version policy, delete uncertain evidence, or start the next milestone without explicit user authorization.

## Current permanent product rules

- Product identity remains `INK v0.1` until the user explicitly changes it.
- Historical version evidence is preserved; no global rewrite.
- `FORMAT_VERSION = 4` is document-format identity, not product version.
- FLORA is optional, not deleted.
- AI / Recipe are not automatically optional just because FLORA is.
- Windows self-hosted Chrome/Edge Node-free Runtime remains the authoritative startup compatibility gate unless governance is explicitly changed.
- REVIEW PASS is not Certified.

## Review completion

The current Review Work Order defines exact required outputs and decision classes. When those are complete, obey its STOP RULE.
